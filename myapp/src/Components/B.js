/* import React from 'react';

const  B =({CurrentFriends})=> {

  return (
    <div>
    {CurrentFriends.map((Data,index)=>{
      return(
       <div key={index}>
    <h4>{Data.names}</h4>
    </div> 
      )})
  }
   <p>hello</p>
   </div>
      )}

export default B;
 */